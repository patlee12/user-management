{
  "extends": "../tsconfig.json", // Extends the base tsconfig at the root
  "exclude": ["node_modules", "test", "**/*spec.ts"], // Exclude test files
  "compilerOptions": {
    "rootDir": "./", // Root directory for the backend app
    "baseUrl": "./", // Base directory for backend imports
    "outDir": "./dist", // Build output for backend app
    "incremental": true
  },
  "include": ["src/**/*", "prisma/**/*", "prisma/seed.ts"]
}
